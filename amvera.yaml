meta:
  environment: docker
  toolchain: default
build:
  image: Dockerfile
run:
  containerPort: 8080
  env:
    - name: GHTOKEN
      value: ${{ secrets.GHTOKEN }}
    - name: PORT
      value: "8080"
